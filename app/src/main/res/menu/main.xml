<menu xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools"
    tools:context=".MainActivity" >

    <item android:id="@+id/prevBtn"
        android:title="@string/prev_label"
        android:icon="@drawable/ic_action_previous"
        android:orderInCategory="100"
        android:showAsAction="ifRoom" />

    <item android:id="@+id/playBtn"
        android:title="@string/play_label"
        android:icon="@drawable/ic_action_play"
        android:orderInCategory="100"
        android:showAsAction="ifRoom" />

    <item android:id="@+id/stopBtn"
        android:title="@string/stop_label"
        android:icon="@drawable/ic_action_stop"
        android:orderInCategory="100"
        android:showAsAction="ifRoom" />

    <item android:id="@+id/nextBtn"
        android:title="@string/next_label"
        android:icon="@drawable/ic_action_next"
        android:orderInCategory="100"
        android:showAsAction="ifRoom" />

</menu>
